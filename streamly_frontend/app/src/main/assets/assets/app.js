// Global JS for all screens
document.addEventListener('DOMContentLoaded', () => {
  // Annotate media control buttons if any
  // This project uses absolutely positioned elements from Figma; no interactions specified.
  console.log('App initialized');
});
