<?xml version="1.0" encoding="utf-8"?>
<!-- Ripple without a rectangular mask so it doesn't introduce a box behind pill-shaped items -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/ocean_secondary">
    <!-- No mask item ensures ripple respects the view's outline/background (pill) -->
</ripple>
