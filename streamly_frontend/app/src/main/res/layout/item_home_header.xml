<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/header_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:clipToPadding="false"
    android:clipChildren="false"
    android:paddingTop="0dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/brand_text_scrolling"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Claro-video"
        android:textColor="@color/ocean_text"
        android:textStyle="bold"
        android:textSize="20sp"
        android:fontFamily="sans-serif-medium"
        android:includeFontPadding="false"
        android:ellipsize="end"
        android:singleLine="true"
        android:layout_marginEnd="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/top_nav_background_scrolling"
        app:layout_constraintTop_toTopOf="@id/top_nav_background_scrolling"
        app:layout_constraintBottom_toBottomOf="@id/top_nav_background_scrolling"/>

    <android.view.View
        android:id="@+id/top_nav_background_scrolling"
        android:layout_width="0dp"
        android:layout_height="46dp"
        android:background="@drawable/bg_top_nav_container"
        android:alpha="1.0"
        app:layout_constraintWidth_percent="0.62"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/top_nav_recycler_scrolling"
        android:layout_width="0dp"
        android:layout_height="46dp"
        android:paddingStart="6dp"
        android:paddingEnd="6dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:clipToPadding="false"
        android:clipChildren="false"
        android:overScrollMode="never"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:contentDescription="@string/top_navigation"
        android:background="@android:color/transparent"
        app:layout_constraintTop_toTopOf="@id/top_nav_background_scrolling"
        app:layout_constraintBottom_toBottomOf="@id/top_nav_background_scrolling"
        app:layout_constraintStart_toStartOf="@id/top_nav_background_scrolling"
        app:layout_constraintEnd_toEndOf="@id/top_nav_background_scrolling" />

</androidx.constraintlayout.widget.ConstraintLayout>
