<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/header_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:clipToPadding="false"
    android:clipChildren="false"
    android:paddingTop="0dp"
    android:paddingBottom="4dp">

    <!-- Brand text -->
    <TextView
        android:id="@+id/brand_text_scrolling"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Claro-video"
        android:textColor="@color/ocean_text"
        android:textStyle="bold"
        android:textSize="20sp"
        android:fontFamily="sans-serif-medium"
        android:includeFontPadding="true"
        android:ellipsize="none"
        android:singleLine="true"
        android:layout_marginEnd="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/top_nav_container_scrolling"
        app:layout_constraintBottom_toBottomOf="@id/top_nav_container_scrolling"
        app:layout_constraintEnd_toStartOf="@id/top_nav_container_scrolling" />

    <!-- Top nav pill container: wrap_content width, exact 2dp padding, centers horizontally -->
    <FrameLayout
        android:id="@+id/top_nav_container_scrolling"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_top_nav_container"
        android:padding="3dp"
        android:clipToPadding="true"
        android:clipChildren="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Horizontal RecyclerView for top nav tabs (wraps content) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/top_nav_recycler_scrolling"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:paddingStart="0dp"
            android:paddingEnd="0dp"
            android:paddingTop="0dp"
            android:paddingBottom="0dp"
            android:clipToPadding="false"
            android:clipChildren="false"
            android:overScrollMode="never"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:contentDescription="@string/top_navigation"
            android:background="@android:color/transparent" />
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
